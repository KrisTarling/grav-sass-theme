{% extends 'partials/base.html.twig' %}
{% block content %}
	{% if hour >= 9 and hour < 17 %}
    <p>Time for cookies!</p>
{% else %}
    <p>Time to bake some cookies!</p>
{% endif %}
    {{ page.content }}
{% endblock %}
